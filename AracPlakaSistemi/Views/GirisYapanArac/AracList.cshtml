@model IPagedList<AracPlakaSistemi.ViewModels.Admin.GirisYapanAracListViewModel>


<div class="products-list-tools clearfix">


    <a class="btn btn-primary btn-sm"
       data-modal-form="true"
       data-modal-size="modal-lg"
       data-modal-prepend="tbody#GirisYapanAracList"
       data-modal-title="Araç Ekle"
       data-hook-beforeunload="true"
       data-modal-save-message="Araç giriş yaptı"
       href="@Url.Action("Add", "GirisYapanArac")">
        <i class="fa fa-plus"></i>Giriş Yapan Araç Ekle
    </a>





    <div class="pull-right hidden-xs hidden-sm">
        @if (Model.Any())
        {
            <div style="margin: 20px 0; float: left !important;">
                @((Model.PageIndex * Model.PageSize) + 1)-@((Model.PageIndex * Model.PageSize) + Model.Count())/@Model.TotalItemCount
            </div>
        }

        &nbsp;
        @Html.Pager(Model.PageSize, Model.PageNumber, Model.TotalItemCount, new AjaxOptions { UpdateTargetId = "GirisYapanAracListContainer" }).Options(o => o
            .Action("AracList")
            .DisplayTemplate("BootstrapPage")
            .MaxNrOfPages(6)
            .AlwaysAddFirstPageNumber()
            )
    </div>
</div>
<table class="table table-bordered">
    <thead>
        <tr>


            <th>Araç Plaka</th>
            <th>Giriş Yaptığı Kapı</th>
            <th>Giriş Yaptığı Tarih</th>
            <th style="width:50px">Sil</th>





        </tr>
    </thead>
    <tbody id="GirisYapanAracList">
        @Html.DisplayFor(m => m)


    </tbody>

</table>
<div class="pull-left">

    @Html.Pager(Model.PageSize, Model.PageNumber, Model.TotalItemCount, new AjaxOptions { UpdateTargetId = "GirisYapanAracListContainer" }).Options(o => o
        .Action("AracList")
        .DisplayTemplate("BootstrapPage")
        .MaxNrOfPages(6)
        .AlwaysAddFirstPageNumber()


        )
</div>
@if (Model.Any())
{
    <div style="margin: 20px 0; float: left !important;">
        @((Model.PageIndex * Model.PageSize) + 1)-@((Model.PageIndex * Model.PageSize) + Model.Count())/@Model.TotalItemCount
    </div>
}

